# Outputs directory

Generated outputs are written here when the analysis scripts are run.

Do not commit large generated files, model artifacts, MIMIC-derived outputs, or
intermediate analysis tables unless the journal repository policy explicitly
requires selected non-sensitive example outputs.
